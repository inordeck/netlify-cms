backend:
  name: github
  repo: https://github.com/inordeck/netlify-cms.git

media_folder: media_folder

collections:
  - name: post
    label: Post
    folder: posts
    create: true
    fields:
      - {name: title, label: Title}
      - {name: body, label: Body, widget: markdown}